<% content_for :title do %>
   <% "Welcome #{current_admin.first_name}" if admin_signed_in? %>
   <% "Welcome #{current_member.first_name}" if member_signed_in? %>
<% end %>

<%= render 'partials/member_dashboard' if member_signed_in? %>
<%= render 'partials/admin_dashboard' if admin_signed_in? %>
